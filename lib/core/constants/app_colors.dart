import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFFFD700); // Gold
  static const Color background = Color(0xFF0A0E27); // Deep space blue
  static const Color surface = Color(0xFF1A1F3A); // Card background
  static const Color accent = Color(0xFF4ECDC4); // Cyan
  
  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0B0B0);
  
  // UI Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF5252);
  static const Color warning = Color(0xFFFFC107);
}
